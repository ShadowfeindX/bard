specVersion: "v1beta"
serviceId: "bard"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "bard-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
